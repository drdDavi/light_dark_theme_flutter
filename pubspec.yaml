#a clean pub file will all the dev packages

name: light_dark_theme_flutter
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  hive: ^1.4.1+1
  #used to store local state
  path_provider: ^1.6.5
  provider: ^4.3.1
   #used to manage global app state
  cupertino_icons: ^0.1.3


dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true
